<?xml version="1.0"?>

<odoo>

    <record id="approval_workflow_root" model="ir.module.category">
        <field name="name">Approval Workflow</field>
        <field name="description">Helps you handle your Basic needs.</field>
    </record>

    <record id="group_approve_payment" model="res.groups">
        <field name="name">Payment Approve</field>
        <field name="category_id" ref="approval_workflow_root"/>
    </record>

    <record id="group_approve_invoice_bill" model="res.groups">
        <field name="name">Invoice Bill Approve</field>
        <field name="category_id" ref="approval_workflow_root"/>
    </record>

    <record id="group_review_payroll_batch" model="res.groups">
        <field name="name">Salary Batch Review</field>
        <field name="category_id" ref="approval_workflow_root"/>
    </record>

    <record id="group_approve_payroll_batch" model="res.groups">
        <field name="name">Salary Batch Approve</field>
        <field name="category_id" ref="approval_workflow_root"/>
    </record>

</odoo>